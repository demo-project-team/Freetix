// import { Request, Response } from "express";

// export const createBooking = async (req:Request, res : Response) => {
//     try {
//         const booking = await.prisma.booking.cre
//     } catch (error) {
//         res.status(500).json({error, message : "Internal server error"})
//     }
// }